<div id="wrapper">
      <h1>Note Scraper</h1>
      <h2><i>If you can do this...the homework should be no problem.</i></h2>
      {{#each data}}
      <h3 data-id="{{_id}}">{{title}}</h3>
      <a href="{{link}}"><span>Read Article</span></a>
      {{/each}}
      <div id="notes"></div>
    </div>