<div id="wrapper">
      <h1>Note Scraper</h1>
      <h2><i>If you can do this...the homework should be no problem.</i></h2>
      {{#each data}}
      <h3>{{title}}</h3>
      {{/each}}
      <div id="notes"></div>
    </div>